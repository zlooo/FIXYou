description 'Api module of FIXYou project, as the name would suggest contains APIs shared between all FIXYou modules'

dependencies {
    api group: 'io.netty', name: 'netty-buffer', version: "$nettyVersion"
    api group: 'com.github.vsonnier', name: 'hppcrt', version: "$hppcrtVersion"
}